# Kinetic opening / 15 September 2026

The photographic-only opening was too restrained for the user's motion references. The replacement is an original SVG contour-lips composition with moving display lettering, a color cut and approved project photography. No emoji glyphs, generated raster images, borrowed reference artwork or animation framework were added.

## Local evidence

- `kinetic-motion.js`: five surfaces passed automatic idle motion after six seconds, changing SVG path geometry, photographic-cut transforms, footer pause/resume, reduced-motion recovery, offscreen suspension and static print. No page errors.
- `submission-preflight.js`: 17 routes, 77 downloads, four widths, no reported missing downloads, emoji-capable UI glyphs, frame collisions or page overflow.
- Motion surfaces additionally checked at 320, 390, 768, 1024 and 1440 pixels.
- Walked landing task to company page, all three identity selections, project dialogs and keyboard-focus return, then back to the task.
- Inspected desktop Editorial, Signal, Studio and assignment opening; phone company and task motion previews. A separate source/screenshot reviewer found no actionable issue.
- Three landing stills refreshed. Landing PDF re-exported and all six pages inspected after correcting a split graphic. Existing campaign/application exports and unrelated PDFs remain unchanged.
- PDF validator checked all 20 PDFs / 70 pages for blank text, watermarks, symbols, preserved quotation values and public links. The validator now writes only when a local link needs normalization.

## Boundaries

Browser checks use Chromium with desktop and phone viewport sizes, not a physical iPhone/Safari certification. Functional checks do not establish aesthetic approval. SVG morphing runs natively; this is not a Remotion video render. Essential text stays outside the moving decorative artwork. Motion pauses in reduced-motion mode by design.

Screenshots and rendered PDF pages are in the original workspace's `output/playwright/kinetic-*` evidence files, outside the public site package.
